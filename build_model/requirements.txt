# LSTM 模型训练与预测所需依赖（适配版本）
# 安装: pip install -r build_model/requirements.txt

# 与 TensorFlow 2.10–2.15 兼容，避免 NumPy 2.x 导致的 _ARRAY_API 错误
numpy>=1.23.0,<2.0.0

# 使用 2.10–2.15 与 numpy<2 稳定配合
tensorflow>=2.10.0,<2.16.0

# 数据处理与评估
pandas>=1.3.0,<2.2.0
scikit-learn>=1.0.0,<1.5.0

# 若需使用 NumPy 2.x，请改用:
# numpy>=2.0.0
# tensorflow>=2.16.0
